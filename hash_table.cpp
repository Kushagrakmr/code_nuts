class hashTable {
  public:
  linkedList[] data; //Linked list of Person Objects
  boolean put(string key, Person value);
  int hashcode = getHashCode(string key);
  linkedList list = data[index];
  list.insert(key.value); //Check for duplicates

};

int hashTable::getHashCode(string key) {
  //Some computation
}